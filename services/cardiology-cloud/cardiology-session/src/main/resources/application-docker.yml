# Docker Compose 生产（profile: docker）
# 业务配置来自 Nacos：cardiology-session-server.yaml

spring:
  config:
    import: optional:nacos:${spring.application.name}.yaml
  cloud:
    nacos:
      server-addr: nacos:8848
      config:
        enabled: true
      discovery:
        enabled: true
