# Docker Compose 生产（profile: docker）

spring:
  cloud:
    nacos:
      server-addr: nacos:8848
      config:
        enabled: false
      discovery:
        enabled: true
  data:
    redis:
      host: redis
      port: 6379
  rabbitmq:
    host: rabbitmq
    port: 5672

cardiology:
  ai-agent:
    base-url: http://ai-agent:8000/api/cardiology/
