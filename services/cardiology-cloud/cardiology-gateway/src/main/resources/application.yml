# 默认 fallback；路由 / JWT / CORS 以 Nacos cardiology-gateway-server.yaml 为准
server:
  port: 30000

spring:
  profiles:
    default: local
  data:
    redis:
      host: 127.0.0.1
      port: 6379
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      discovery:
        locator:
          enabled: false
      routes:
        - id: cardiology-auth
          uri: lb://cardiology-auth-server
          predicates:
            - Path=/auth/**
          filters:
            - name: Retry
              args:
                retries: 1
                statuses: BAD_GATEWAY,SERVICE_UNAVAILABLE
        - id: cardiology-session-understanding
          uri: lb://cardiology-session-server
          predicates:
            - Path=/chat/generalUnderstanding/**
          filters:
            - name: Retry
              args:
                retries: 1
                statuses: BAD_GATEWAY,SERVICE_UNAVAILABLE
        - id: cardiology-session
          uri: lb://cardiology-session-server
          predicates:
            - Path=/chat/**
          filters:
            - name: Retry
              args:
                retries: 1
                statuses: BAD_GATEWAY,SERVICE_UNAVAILABLE

auth:
  guest:
    key: "cardiology:guest:session:"

jwt:
  sign-key: zengxiangrui-daming-cardiology-intelligent-agent-platform

cardiology:
  auth:
    exclude-paths:
      - /auth/**
