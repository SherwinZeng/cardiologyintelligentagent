# Docker Compose 生产（profile: docker）

spring:
  cloud:
    nacos:
      server-addr: nacos:8848
      config:
        enabled: false
      discovery:
        enabled: true
  data:
    redis:
      host: redis
      port: 6379
