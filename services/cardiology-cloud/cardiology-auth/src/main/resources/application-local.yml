# profile=local：业务配置从 Nacos public 命名空间读取
spring:
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
      config:
        enabled: true
        file-extension: yaml
        group: DEFAULT_GROUP
      discovery:
        enabled: true
