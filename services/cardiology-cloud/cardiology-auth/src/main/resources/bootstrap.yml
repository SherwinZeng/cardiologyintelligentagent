spring:
  application:
    name: cardiology-auth-server
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      username: nacos
      password: zxr18072164990
      config:
        file-extension: yaml
  config:
    import: optional:nacos:${spring.application.name}.yaml
